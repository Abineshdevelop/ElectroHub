<style>
  /* Filter tabs — All, Delivered, Cancelled, category filters */
  .admin-filter-tab {
    display: inline-flex; align-items: center; justify-content: center;
    padding: 6px 14px; border-radius: 6px; font-size: 10px; font-weight: 800;
    text-transform: uppercase; letter-spacing: 0.04em;
    border: 1.5px solid #e5e7eb; background: #fff; color: #4b5563;
    cursor: pointer; transition: all .15s; white-space: nowrap;
  }
  .admin-filter-tab:hover:not(.active) { border-color: #86efac; color: #15803d; background: #f0fdf4; }
  .admin-filter-tab.active { background: #16a34a; border-color: #16a34a; color: #fff; box-shadow: 0 2px 6px rgba(22, 163, 74, .2); }

  /* Table action links — text color only (View / Edit / Block / Unblock / Delete) */
  .action-btns {
    display: inline-flex; align-items: center; white-space: nowrap; flex-wrap: nowrap; gap: 0;
  }
  .action-btns .sep { color: #d1d5db; user-select: none; padding: 0 5px; flex-shrink: 0; }
  .action-btns button {
    white-space: nowrap; background: none; border: none; padding: 0;
    cursor: pointer; font-size: 0.875rem; font-weight: 500; flex-shrink: 0;
    transition: color .15s;
  }
  .admin-btn-block, .vt-btn-block {
    color: #ca8a04;
  }
  .admin-btn-block:hover, .vt-btn-block:hover { color: #a16207; }
  .admin-btn-unblock, .vt-btn-unblock {
    color: #16a34a;
  }
  .admin-btn-unblock:hover, .vt-btn-unblock:hover { color: #15803d; }
  .admin-btn-delete {
    color: #ef4444;
  }
  .admin-btn-delete:hover { color: #b91c1c; }

  /* Status pills — dot + soft background (Active / Inactive / Blocked / Expired) */
  .status-pill, .vt-badge-active, .vt-badge-inactive, .vt-badge-blocked {
    display: inline-flex; align-items: center; gap: 6px;
    padding: 4px 10px; border-radius: 9999px; font-size: 12px; font-weight: 500;
    white-space: nowrap; flex-shrink: 0;
  }
  .status-pill::before, .vt-badge-active::before, .vt-badge-inactive::before, .vt-badge-blocked::before {
    content: ''; width: 6px; height: 6px; border-radius: 50%; flex-shrink: 0;
  }
  .status-pill-active, .vt-badge-active { background: #dcfce7; color: #15803d; }
  .status-pill-active::before, .vt-badge-active::before { background: #22c55e; }
  .status-pill-inactive, .vt-badge-inactive { background: #fee2e2; color: #dc2626; }
  .status-pill-inactive::before, .vt-badge-inactive::before { background: #ef4444; }
  .status-pill-blocked, .vt-badge-blocked { background: #fee2e2; color: #dc2626; }
  .status-pill-blocked::before, .vt-badge-blocked::before { background: #ef4444; }
  .status-pill-expired { background: #f3f4f6; color: #6b7280; }
  .status-pill-expired::before { background: #9ca3af; }
</style>
